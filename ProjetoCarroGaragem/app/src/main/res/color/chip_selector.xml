<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor quando o chip está selecionado -->
    <item android:color="@color/primaryColor" android:state_checked="true"/>
    <!-- Cor quando o chip está pressionado -->
    <item android:color="@color/primaryDark" android:state_pressed="true"/>
    <!-- Cor padrão -->
    <item android:color="@color/textSecondary"/>
</selector>